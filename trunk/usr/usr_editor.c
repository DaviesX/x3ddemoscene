/*! \file usr/usr_editor.c
    \brief applications to control the default editor. */
#include <x3d/common.h>
#include <usr/usr_editor.h>


/** \brief set up the default editor
 *
 * Note that: this function will link the GUI driver with the Core activex. Also notice that this function has to be called before the default editor is run
 * \param void
 * \return void
 */
__dlexport void usr_editor_setup_default ( void )
{
}
